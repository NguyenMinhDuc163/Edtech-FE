name: Build and Deploy to Server

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  DOCKER_IMAGE: ${{ secrets.DOCKER_IMAGE }}
  DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
  SERVICE_NAME: ${{ secrets.SERVICE_NAME }}

jobs:
  build-and-push:
    name: Build Docker Image and Push to Docker Hub
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.set_version.outputs.version }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set version from package.json
        id: set_version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Building version: $VERSION"

      - name: Create .env file
        run: |
          if [ -z "${{ secrets.VITE_API_URL }}" ]; then
            echo "VITE_API_URL=https://edtech_service.nguyenduc.click" > .env
          else
            echo "VITE_API_URL=${{ secrets.VITE_API_URL }}" > .env
          fi
        shell: bash

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Debug - Check secrets availability
        run: |
          echo "Checking if secrets are available..."
          if [ -z "${{ secrets.DOCKERHUB_USERNAME }}" ]; then
            echo "[ERROR] DOCKERHUB_USERNAME is empty or not set"
          else
            echo "[INFO] DOCKERHUB_USERNAME is set (length: ${#DOCKERHUB_USERNAME})"
          fi

          if [ -z "${{ secrets.DOCKERHUB_TOKEN }}" ]; then
            echo "[ERROR] DOCKERHUB_TOKEN is empty or not set"
          else
            echo "[INFO] DOCKERHUB_TOKEN is set (length: ${#DOCKERHUB_TOKEN})"
          fi
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE }}:${{ steps.set_version.outputs.version }}
            ${{ env.DOCKER_IMAGE }}:latest
          cache-from: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache
          cache-to: type=registry,ref=${{ env.DOCKER_IMAGE }}:buildcache,mode=max

  deploy:
    name: Deploy to Server
    needs: build-and-push
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy docker-compose.yml to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: 22
          source: "docker-compose.yml"
          target: ${{ env.DEPLOY_PATH }}

      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1.2.0
        env:
          DOCKER_IMAGE: ${{ env.DOCKER_IMAGE }}
          VERSION: ${{ needs.build-and-push.outputs.version }}
          DEPLOY_PATH: ${{ env.DEPLOY_PATH }}
          SERVICE_NAME: ${{ env.SERVICE_NAME }}
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: 22
          envs: DOCKER_IMAGE,VERSION,DEPLOY_PATH,SERVICE_NAME
          script: |
            set -e
            cd ${DEPLOY_PATH}

            echo "Pulling new image: ${DOCKER_IMAGE}:${VERSION}"
            docker pull ${DOCKER_IMAGE}:${VERSION}

            echo "Updating docker-compose.yml with new version"
            sed -i "s|image: ${DOCKER_IMAGE}:.*|image: ${DOCKER_IMAGE}:${VERSION}|g" docker-compose.yml

            echo "Stopping old container"
            docker compose down

            echo "Starting new container"
            docker compose up -d

            echo "Waiting for container to be healthy..."
            sleep 10

            echo "Checking container status"
            docker compose ps

            echo "Cleaning up old images"
            docker image prune -af --filter "until=48h"

            echo "Deployment completed successfully!"

      - name: Verify deployment
        uses: appleboy/ssh-action@v1.2.0
        env:
          DEPLOY_PATH: ${{ env.DEPLOY_PATH }}
          SERVICE_NAME: ${{ env.SERVICE_NAME }}
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: 22
          envs: DEPLOY_PATH,SERVICE_NAME
          script: |
            cd ${DEPLOY_PATH}

            echo "Checking if container is running..."
            if [ "$(docker compose ps -q ${SERVICE_NAME})" ]; then
              echo "[SUCCESS] Container is running"
              docker compose logs --tail=50 ${SERVICE_NAME}
            else
              echo "[ERROR] Container is not running!"
              exit 1
            fi
